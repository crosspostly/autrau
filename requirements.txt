# Core (required)
# IMPORTANT: starlette < 0.50 is required. FastAPI 0.121.x passes
# on_startup/on_shutdown to Starlette, which were removed in 1.x.
fastapi>=0.115,<0.123
starlette>=0.40,<0.50
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9
huggingface-hub>=0.20

# Default provider (installed by default)
faster-whisper>=1.0,<2.0

# Optional providers — installed on demand via the UI
# whisper.cpp via pywhispercpp (CPU-only, very lightweight):
#     pip install pywhispercpp
#
# Parakeet v3 (NVIDIA GPU + CUDA, SOTA 2025-2026):
#     pip install -U nemo_toolkit[asr]
# Note: nemo_toolkit pulls in torch (~2-3 GB) and has strict CUDA requirements.

# v1.5: Translation (optional, installed on demand via /api/translate/install-argos)
# argostranslate — local EN<->RU model (~336 MB total), 100+ language pairs available
# langdetect — auto-detect source language (used by argos provider)
# Both are installed automatically when the user clicks "📥 Install Argos" in the UI.

# v1.5.7: yt-dlp — download audio from YouTube/Vimeo/Twitter/etc (~3 MB)
# Required for `POST /api/yt-dlp` (URL → audio → transcribe).
# If missing, the endpoint returns a helpful error.
yt-dlp>=2024.0.0

# v1.7: Telegram agent bot (optional, only needed for `start_telegram_bot.bat`).
# Provides voice messages → auto-transcribe, commands, agent-mode /ask.
# Install manually if you want the bot:
#     pip install "python-telegram-bot>=20.0,<22.0"
# Not in base requirements — bot is opt-in (needs TELEGRAM_BOT_TOKEN).

